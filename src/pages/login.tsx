import { Button } from "components/elements";
import { Container } from "@material-ui/core";

const Login = () => {
  return (
    <Container>
      <form>
        <Button label="Login" />
      </form>
    </Container>
  );
};

export default Login;
